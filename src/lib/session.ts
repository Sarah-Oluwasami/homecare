/**
 * Who is signed in.
 *
 * One constant because two places show it — the sidebar and the topbar — and a
 * third now attributes a written resolution to it. A hardcoded name in the
 * resolution form had already drifted to a different real coordinator, so a
 * clinical conclusion typed by one person was being credited to another.
 */
export const SIGNED_IN = 'Sarah Jenkins'
export const SIGNED_IN_ROLE = 'Operations Manager'
